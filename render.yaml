services:
  - type: web
    name: file-forge-toolkit
    env: python
    buildCommand: pip install -r smart_file_converter/requirements.txt
    startCommand: gunicorn smart_file_converter.app:app
    aptPackages:
      - tesseract-ocr
